fragment FeaturedTileBlockData on FeaturedTileBlock {
  title: TileTitle
  TileDescription
  buttontext: TileButtonText
  backgroundImage {
    ...ReferenceData
  }
  Position
  CTA: Button {
    ...CTAButtonBlockData
  }
}
